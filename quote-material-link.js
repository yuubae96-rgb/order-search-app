'use strict';

let QUOTE_MATERIALS=[];
let QUOTE_MATERIAL_PRICES=[];
let QUOTE_MATERIAL_SNAPSHOT=null;
let QUOTE_MATERIAL_LOAD_TIMER=null;
let QUOTE_MATERIAL_AUTH_RECOVERY=false;

function qNum(v){const n=Number(v);return Number.isFinite(n)?n:0;}
function uniq(arr){return [...new Set(arr.filter(v=>v!==null&&v!==undefined&&String(v)!=='' ).map(v=>String(v)))];}
function quoteLatestPrice(materialId){return QUOTE_MATERIAL_PRICES.filter(p=>String(p.material_id)===String(materialId)).sort((a,b)=>String(b.effective_from||'').localeCompare(String(a.effective_from||''))||Number(b.id)-Number(a.id))[0]||null;}
function parsePurchaseDimensions(material){if(material.width_mm&&material.length_value)return{widthMm:qNum(material.width_mm),lengthMm:material.length_unit==='m'?qNum(material.length_value)*1000:qNum(material.length_value)};const text=String(material.purchase_form||'');let m=text.match(/(\d+(?:\.\d+)?)\s*[×xX]\s*(\d+(?:\.\d+)?)\s*板/);if(m)return{widthMm:qNum(m[1]),lengthMm:qNum(m[2])};m=text.match(/幅\s*(\d+(?:\.\d+)?)\s*mm\s*[×xX]\s*(\d+(?:\.\d+)?)\s*m\s*巻/);if(m)return{widthMm:qNum(m[1]),lengthMm:qNum(m[2])*1000};return null;}
function mapMaterialToLegacy(m){const name=String(m.name||''),spec=String(m.spec||'');if(name.includes('カラーアルマイト')||String(m.surface_finish||'').includes('カラーアルマイト'))return'カラーアルマイト';if(name.includes('アルミ')||/^A1\d{3}/.test(spec))return'アルミ';if(name.includes('ステンレス')||/^SUS/.test(spec))return'ステンレス';if(name.includes('真鍮')||/^C2801/.test(spec))return'真鍮';return'その他';}
function escOpt(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
function setOptions(id,values,placeholder){const el=document.getElementById(id);if(!el)return;el.innerHTML=`<option value="">${placeholder}</option>`+values.map(v=>`<option value="${escOpt(v)}">${escOpt(v)}</option>`).join('');el.disabled=!values.length;}
function currentMaterial(){const id=document.getElementById('f_materialMaster')?.value;return QUOTE_MATERIALS.find(m=>String(m.id)===String(id))||null;}
function calculateQuoteMaterialSnapshot(){const m=currentMaterial(),info=document.getElementById('materialMasterInfo');if(!m){QUOTE_MATERIAL_SNAPSHOT=null;if(info)info.innerHTML='材料を上から順に選ぶと、材質・板厚と登録済み単価を自動で反映します。';return;}const p=quoteLatestPrice(m.id),w=qNum(document.getElementById('f_sizeH')?.value),h=qNum(document.getElementById('f_sizeV')?.value),qty=Math.max(1,qNum(document.getElementById('f_quantity')?.value)||1),dims=parsePurchaseDimensions(m);let cost=null,detail='';if(p&&w>0&&h>0&&dims?.widthMm>0&&dims?.lengthMm>0){const ratio=(w*h*qty)/(dims.widthMm*dims.lengthMm);if(p.price_basis==='kg'&&qNum(m.unit_weight_kg)>0){const kg=ratio*qNum(m.unit_weight_kg);cost=kg*qNum(p.price);detail=`必要重量 約${kg.toFixed(3)}kg`;}else{cost=ratio*qNum(p.price);detail=`購入単位の面積比 約${(ratio*100).toFixed(2)}%`;}}QUOTE_MATERIAL_SNAPSHOT={materialId:m.id,priceId:p?.id||null,unitPrice:p?qNum(p.price):null,estimatedCost:cost==null?null:Math.round(cost)};if(info){const pt=p?`最新単価：<b>${qNum(p.price).toLocaleString('ja-JP')}円</b> / ${p.price_basis==='kg'?'kg':(m.stock_unit||'単位')}　適用日 ${p.effective_from}`:'<b>単価未登録</b>';const ct=cost==null?'サイズ・数量・購入寸法などが揃うと材料原価の参考値を表示します。':`材料原価（ロス未加算）：<b>${Math.round(cost).toLocaleString('ja-JP')}円</b>　${detail}`;info.innerHTML=`${pt}<br>${ct}`;}}
function applyMasterMaterial(){const m=currentMaterial();if(!m){calculateQuoteMaterialSnapshot();return;}const legacy=mapMaterialToLegacy(m),mat=document.getElementById('f_material'),other=document.getElementById('f_materialOther'),th=document.getElementById('f_thickness');if(mat){mat.value=legacy;mat.dispatchEvent(new Event('change',{bubbles:true}));}if(other&&legacy==='その他')other.value=m.name||m.spec||'';if(th)th.value=m.thickness_mm??'';calculateQuoteMaterialSnapshot();}

function resetBelow(level){const order=['f_materialSpec','f_materialThickness','f_materialSize'];const start=order.indexOf(level);for(let i=Math.max(0,start);i<order.length;i++){const el=document.getElementById(order[i]);if(el){el.innerHTML='<option value="">選択してください</option>';el.disabled=true;}}const master=document.getElementById('f_materialMaster');if(master)master.value='';QUOTE_MATERIAL_SNAPSHOT=null;}
function syncMaterialCascade(){const name=document.getElementById('f_materialName')?.value||'',spec=document.getElementById('f_materialSpec')?.value||'',th=document.getElementById('f_materialThickness')?.value||'',size=document.getElementById('f_materialSize')?.value||'';let list=QUOTE_MATERIALS.slice();if(name)list=list.filter(m=>String(m.name||'')===name);if(spec)list=list.filter(m=>String(m.spec||'')===spec);if(th)list=list.filter(m=>String(m.thickness_mm??'')===th);if(size)list=list.filter(m=>String(m.purchase_form||'')===size);const master=document.getElementById('f_materialMaster');if(master){master.value=list.length===1?String(list[0].id):'';}if(list.length===1)applyMasterMaterial();else calculateQuoteMaterialSnapshot();}
function onNameChange(){const name=document.getElementById('f_materialName').value;resetBelow('f_materialSpec');const list=QUOTE_MATERIALS.filter(m=>String(m.name||'')===name);setOptions('f_materialSpec',uniq(list.map(m=>m.spec||m.name)),'規格を選択');syncMaterialCascade();}
function onSpecChange(){const name=document.getElementById('f_materialName').value,spec=document.getElementById('f_materialSpec').value;resetBelow('f_materialThickness');const list=QUOTE_MATERIALS.filter(m=>String(m.name||'')===name&&String(m.spec||m.name)===spec);const ths=uniq(list.map(m=>m.thickness_mm==null?'（厚みなし）':String(m.thickness_mm)));setOptions('f_materialThickness',ths,'板厚を選択');if(ths.length===1){document.getElementById('f_materialThickness').value=ths[0];onThicknessChange();}else syncMaterialCascade();}
function onThicknessChange(){const name=document.getElementById('f_materialName').value,spec=document.getElementById('f_materialSpec').value,th=document.getElementById('f_materialThickness').value;resetBelow('f_materialSize');const list=QUOTE_MATERIALS.filter(m=>String(m.name||'')===name&&String(m.spec||m.name)===spec&&String(m.thickness_mm==null?'（厚みなし）':m.thickness_mm)===th);const sizes=uniq(list.map(m=>m.purchase_form||'（サイズ未登録）'));setOptions('f_materialSize',sizes,'サイズを選択');if(sizes.length===1){document.getElementById('f_materialSize').value=sizes[0];onSizeChange();}else syncMaterialCascade();}
function onSizeChange(){const name=document.getElementById('f_materialName').value,spec=document.getElementById('f_materialSpec').value,th=document.getElementById('f_materialThickness').value,size=document.getElementById('f_materialSize').value;const list=QUOTE_MATERIALS.filter(m=>String(m.name||'')===name&&String(m.spec||m.name)===spec&&String(m.thickness_mm==null?'（厚みなし）':m.thickness_mm)===th&&String(m.purchase_form||'（サイズ未登録）')===size);const master=document.getElementById('f_materialMaster');if(master)master.value=list.length?String(list[0].id):'';applyMasterMaterial();}
function renderMaterialPicker(){setOptions('f_materialName',uniq(QUOTE_MATERIALS.map(m=>m.name)).sort((a,b)=>a.localeCompare(b,'ja')),'材質を選択');resetBelow('f_materialSpec');const info=document.getElementById('materialMasterInfo');if(!QUOTE_MATERIALS.length&&info)info.innerHTML='材料マスターはまだ0件です。材料・在庫で登録すると、ここから選べるようになります。';else if(info)info.innerHTML='「材質 → 規格 → 板厚 → サイズ」の順に選んでください。';}

function isAuthError(err){return!!err&&(err.status===401||err.code==='PGRST301'||/jwt|token|unauthorized/i.test(String(err.message||'')));}
async function recoverMaterialAuth(){if(QUOTE_MATERIAL_AUTH_RECOVERY)return false;QUOTE_MATERIAL_AUTH_RECOVERY=true;try{const{data,error}=await supabaseClient.auth.refreshSession();if(!error&&data?.session)return true;await supabaseClient.auth.signOut({scope:'local'}).catch(()=>{});if(typeof showAuthGate==='function')showAuthGate('ログイン情報を更新するため、もう一度ログインしてください。');return false;}finally{QUOTE_MATERIAL_AUTH_RECOVERY=false;}}
async function loadQuoteMaterials(retryAuth=true){const nameEl=document.getElementById('f_materialName');if(!nameEl||!window.supabaseClient)return;if(QUOTE_MATERIAL_LOAD_TIMER){clearTimeout(QUOTE_MATERIAL_LOAD_TIMER);QUOTE_MATERIAL_LOAD_TIMER=null;}nameEl.innerHTML='<option value="">材料マスターを読み込み中...</option>';nameEl.disabled=true;try{const[mr,pr]=await Promise.all([supabaseClient.from('materials').select('*').eq('active',true).order('name').order('spec').order('thickness_mm'),supabaseClient.from('material_prices').select('*').order('effective_from',{ascending:false})]);if(mr.error||pr.error){const err=mr.error||pr.error;console.error('材料マスター読込エラー',err);if(retryAuth&&isAuthError(err)){const recovered=await recoverMaterialAuth();if(recovered)return loadQuoteMaterials(false);}nameEl.innerHTML='<option value="">材料マスターの読込に失敗しました</option>';return;}QUOTE_MATERIALS=mr.data||[];QUOTE_MATERIAL_PRICES=pr.data||[];renderMaterialPicker();}catch(err){console.error('材料マスター読込例外',err);nameEl.innerHTML='<option value="">材料マスターを再読み込み中...</option>';QUOTE_MATERIAL_LOAD_TIMER=setTimeout(()=>loadQuoteMaterials(true),1000);}}

async function saveQuoteMaterialSnapshot(id){if(!QUOTE_MATERIAL_SNAPSHOT||!id)return;const s=QUOTE_MATERIAL_SNAPSHOT;const{error}=await supabaseClient.from('estimates').update({material_id:s.materialId,material_price_id:s.priceId,material_unit_price_snapshot:s.unitPrice,material_cost_snapshot:s.estimatedCost}).eq('id',Number(id));if(error)console.error('材料単価スナップショット保存エラー',error);}
function installQuoteMaterialIntegration(){const old=document.getElementById('f_material');if(!old||document.getElementById('f_materialPicker'))return;const row=old.closest('.row2');if(!row)return;const box=document.createElement('div');box.className='field';box.id='f_materialPicker';box.style.marginBottom='12px';box.innerHTML=`<label>材料マスターから選択<span class="opt">推奨</span></label><div style="display:grid;grid-template-columns:1fr 1fr;gap:10px"><select id="f_materialName"><option value="">読み込み中...</option></select><select id="f_materialSpec" disabled><option value="">規格を選択</option></select><select id="f_materialThickness" disabled><option value="">板厚を選択</option></select><select id="f_materialSize" disabled><option value="">サイズを選択</option></select></div><input type="hidden" id="f_materialMaster"><div id="materialMasterInfo" style="font-size:12px;line-height:1.65;margin-top:7px;opacity:.78;">材料マスターを読み込んでいます。</div>`;row.parentNode.insertBefore(box,row);document.getElementById('f_materialName').addEventListener('change',onNameChange);document.getElementById('f_materialSpec').addEventListener('change',onSpecChange);document.getElementById('f_materialThickness').addEventListener('change',onThicknessChange);document.getElementById('f_materialSize').addEventListener('change',onSizeChange);['f_sizeV','f_sizeH','f_quantity'].forEach(id=>document.getElementById(id)?.addEventListener('input',calculateQuoteMaterialSnapshot));if(typeof StorageAPI!=='undefined'&&!StorageAPI.__materialLinked){const add=StorageAPI.add.bind(StorageAPI);StorageAPI.add=async function(o){const saved=await add(o);await saveQuoteMaterialSnapshot(saved&&saved.id);return saved;};const upd=StorageAPI.update.bind(StorageAPI);StorageAPI.update=async function(id,p,e){const r=await upd(id,p,e);if(r&&!r.conflict)await saveQuoteMaterialSnapshot(id);return r;};StorageAPI.__materialLinked=true;}loadQuoteMaterials();}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',installQuoteMaterialIntegration);else setTimeout(installQuoteMaterialIntegration,0);
supabaseClient.auth.onAuthStateChange((_e,s)=>{if(s)setTimeout(()=>loadQuoteMaterials(true),50);});
window.addEventListener('pageshow',()=>setTimeout(()=>loadQuoteMaterials(true),100));
document.addEventListener('visibilitychange',()=>{if(!document.hidden)setTimeout(()=>loadQuoteMaterials(true),100);});
